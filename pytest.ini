[pytest]
# Only look in the top‐level tests/ directory
testpaths = tests

# Add the repo root to Python’s import path before collecting
# (this lets 'import papi.*' resolve to your local folder)
addopts = --import-mode=prepend